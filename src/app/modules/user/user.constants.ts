import { TAccountStatus } from './user.interface';
import { TRole } from './user.interface';

export const Roles: TRole[] = ['student', 'teacher', 'admin'];
export const AccountStatuses: TAccountStatus[] = ['in-progress', 'blocked'];
